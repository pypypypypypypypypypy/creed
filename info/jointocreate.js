module.exports = function jointocreate(client) {
  // Compatibility shim for the deployment entrypoint.
  // The repository currently has no jointocreate implementation file,
  // so this no-op preserves the startup contract while avoiding a crash.
};
